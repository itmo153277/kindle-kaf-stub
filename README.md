# kindle-kaf-stub

Register a flag that Kindle Framework has loaded which enables device locking
even when framework was stopped.

## Build from source

Generate autotools scripts

```sh
sh autogen.sh
```

Configure

```sh
./configure --enable-maintainer-mode
```

Build

```sh
make
```

Install as root

```sh
make install
```
